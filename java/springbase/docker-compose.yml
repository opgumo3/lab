services:
  postgres:
    image: postgres:17
    container_name: springbase-postgres
    environment:
      POSTGRES_DB: springbase
      POSTGRES_USER: springbase
      POSTGRES_PASSWORD: springbase
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data

volumes:
  postgres-data:
